// src/middleware/upload.js
// For now, we'll use a simple upload without external packages
// Later you can add multer for file uploads

const upload = (req, res, next) => {
  // Placeholder for upload functionality
  // We'll implement this later with multer
  next();
};

module.exports = upload;